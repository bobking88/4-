# 四分类基线训练说明

## 训练前的唯一人工步骤

请先打开 `数据集/dataset_review_20260727/contact_sheets` 中的 33 张拼图，
再在 `review_queue.csv` 填写复核结论：

- `keep`：保留；
- `exclude`：剔除，并填写原因；
- `needs_expert`：主体或标签不确定，等待专业复核。

不要修改 `image_id`、`relative_path`、`four_class_label` 或 `split`。该抽检包共
823 张，覆盖 17 种矿物；钛磁铁矿的 23 张合格图片全部纳入复核。

## 训练环境

训练使用项目内的 `.venv-training`，避免影响系统 Python 或 Anaconda。完整依赖见
`requirements-training.txt`。GPU 框架安装并通过 CUDA 检查后，先运行烟雾测试：

```powershell
.\.venv-training\Scripts\python.exe .\scripts\train_mineral_classifier.py --model resnet50 --smoke-run
```

烟雾测试只训练两批样本，目的是确认图片读取、标签、显卡、模型前向传播和结果保存都正常；
它不是正式实验结果。

## 正式实验

人工复核结果应用到数据集后，再按下面的固定划分进行训练，不得重新随机切分：

```powershell
.\.venv-training\Scripts\python.exe .\scripts\train_mineral_classifier.py --model resnet50 --epochs 30 --batch-size 16
.\.venv-training\Scripts\python.exe .\scripts\train_mineral_classifier.py --model efficientnet_b0 --epochs 30 --batch-size 16
```

每个模型建议以不同随机种子重复三次。输出目录会保存：最佳模型、环境信息、每轮指标、
测试集指标和混淆矩阵。论文以 Macro-F1 和目标矿物召回率为主指标。

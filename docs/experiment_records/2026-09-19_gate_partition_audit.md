# 样本外门控监督的训练内部隔离清单

已运行 `scripts/build_gate_supervision_partition.py`，仅在原训练集内部按矿物标签和角色分层、以原重复组为单位分配。输出 `outputs/training/gate_supervision_partition_v1/training_partition.csv` 与 `audit.json`。

| 用途 | 图像数 |
|---|---:|
| 专家拟合 expert_fit | 4,176 |
| 专家停止 expert_stop | 893 |
| 门控监督 gate_fit | 892 |
| 合计 | 5,961 |

四项单元测试通过：输入顺序不影响分配、同组不拆分、拒绝非训练输入、拒绝混合标签或不足三组的分层。输出文件另行只读检查确认 5,961 个图像 ID 唯一且无重复组跨实验子集。原始清单未改写，原 `split` 列仍全部为 train；新增 `experiment_subset` 只是实验用途字段，尚未作为现有训练器的 split 使用。

钛磁铁矿训练图像分为 11/2/2；不能用门控监督的两张图像声称其独立性能得到充分验证。以上数量不是原始下载数量，也未更改既有四分类数据版本。

下一步执行必须从 ImageNet 初始化训练新的专家，而非加载已见过全部训练图像的旧矿物检查点；模型选择只使用 expert_stop。门控监督集不得用于专家训练或停止。还需生成与 892 张门控监督规模及类别分布匹配的训练内对照，确保比较不会把样本量变化混入监督来源效应。

本轮仅完成隔离清单和测试，未启动新专家训练，未获得新性能结果。该设计属于研究控制条件，不作为网络创新贡献。本批和前一批最终门控实验仍保存在本地，Git 写操作此前受自动审批服务额度限制阻断，不能标记为已上传。

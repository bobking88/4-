# 面向钒钛矿相关矿物视觉角色识别的跨粒度后悔监督门控验证网络

## 摘要（工作稿）

公开矿物标本图像具有拍摄条件异构、伴生矿物混杂和类内外观差异显著等特点，直接将其用于钒钛矿相关矿物识别时，目标矿物容易与含钛干扰矿物和金属光泽干扰矿物发生混淆。针对这一问题，本文构建包含 8,529 张图像的四类选矿角色数据集，在固定划分和重复图像分组约束下，提出跨粒度后悔监督门控验证网络（Regret-Supervised Hierarchical Risk-Gated Verification Network, RSG-HRGV-Net）。该网络以共享视觉主干为基础，通过直接角色专家和十七类矿物种类专家生成两条层级证据；门控以共享特征、熵和专家分歧为输入，并以真实类别下的专家损失差构造训练期软监督。双验证器和具有中性区的残差后验校正用于抑制两类困难负样本误入目标类。本文给出凸融合路由后悔上界、软目标逼近和后悔分支局部梯度隔离等性质，并通过逐图局部上界对门控难度进行诊断。三随机种子正式对照显示，RSG-HRGV 的 Accuracy 为 76.45% ± 0.98%、Macro F1 为 74.76% ± 0.90%；总体分类指标的成对簇 Bootstrap 区间跨 0，但平均路由后悔相对 HRGV 降低 1.77 个百分点，95% 区间为 [−2.86, −0.69] 个百分点。在摄影者整体留出的独立三随机种子确认中，路由后悔进一步降低 3.53 个百分点，95% 区间为 [−4.75, −2.17] 个百分点；在严格匹配的 ResNet50 主干替换中，路由后悔降低 3.33 个百分点，95% 区间为 [−4.86, −1.98] 个百分点。9 次高精度检查点重放覆盖 10,242 张图像，B.1/B.2 的数值违反计数均为 0。上述结果支持跨粒度路由机制的受限有效性，而总体分类指标仍未呈现稳定增益。本研究验证了公开标本图像条件下的相关矿物视觉代理识别，不将结果解释为品位预测或工业分选回收率。

**关键词：** 钒钛磁铁矿；矿物图像识别；层级学习；困难负样本；风险门控；选择性识别

## 1 引言（待完成文献核验后扩写）

钒钛磁铁矿资源的综合利用涉及铁、钒和钛等组分的分阶段回收。矿物的颜色、光泽、形态和基质特征能够为预选和人工辅助识别提供视觉线索，但宏观 RGB 图像不能直接揭示元素含量、矿物嵌布状态或微观赋存关系。因此，视觉模型在本研究中被定位为相关矿物外观与选矿角色识别工具，而非 XRD、SEM-EDS、XRF 或化学分析的替代方法。[待核验引用：钛磁铁矿选矿综述]

现有矿物图像研究已经覆盖卷积神经网络、Transformer、多标签识别、细粒度学习和开集识别等方向，钒钛矿和铁矿石的机器视觉分类也已有代表性工作。[待核验引用：宁金论文、矿物 OOD 论文、铁矿多视图融合论文] 这些研究证明了数据驱动视觉特征的可用性，同时也表明，受控工业成像、公开标本图片和跨来源真实样本之间存在明显的任务差异。对于公开标本图片，页面主题矿物并不必然是画面中的唯一矿物，背景、基质和伴生矿物还可能形成非目标线索。

本项目的基线实验进一步显示，四类选矿角色的错误具有结构性。含钛干扰矿物和金属光泽干扰矿物较容易被误判为目标矿物，而简单采用类别加权或 Focal Loss 不能同时改善所有类别。这意味着性能瓶颈不只来自类别数量不平衡，还来自层级证据可靠性和特定困难负样本关系。基于这一观察，本文提出 RSG-HRGV-Net，将直接角色分类、矿物种类分类、样本级证据门控和困难负样本验证统一到同一网络中，并以真实类别下的专家相对损失差直接监督门控路由。

本文贡献如下：

1. 构建面向钒钛矿相关矿物选矿角色的四分类任务，并保留十七类矿物种类标签，使角色监督和种类监督能够共同用于模型训练。
2. 提出 RSG-HRGV-Net：以共享特征、两路熵和 Jensen-Shannon 分歧驱动样本级融合，并以样本级专家后悔构造软门控监督；双验证器进一步建模目标矿物与两类困难负样本之间的局部风险。
3. 从凸融合、软目标和停止梯度计算图出发，给出路由后悔上界、软目标对硬最优门控的指数逼近、局部梯度隔离及局部 B.1 推论；这些公式用于定义可复核的路由机制指标，而非替代总体分类指标。
4. 通过固定测试、摄影者整体留出和 ResNet50 主干替换的三随机种子对照、组件消融、成对簇 Bootstrap 与 9 次高精度检查点重放，分别检验总体性能、困难负样本误入和预定义路由后悔；将机制改善与整体分类性能严格区分。

## 2 数据与任务定义

### 2.1 数据来源与类别体系

数据集由公开矿物标本图片及其可追溯元数据构成，共包含 8,529 张图像。依据矿物在本研究视觉代理任务中的作用，图像被组织为目标矿物、含钛干扰矿物、脉石/废石和金属光泽干扰矿物四类。其中，目标矿物包含钛铁矿、钛磁铁矿和普通磁铁矿代理样本；含钛干扰矿物包括金红石、锐钛矿、榍石和钙钛矿等；脉石/废石和金属光泽干扰类由相应矿物种类组成。普通磁铁矿仅作为外观代理来源，不能等同为钛磁铁矿真实样本。

每张图片保留矿物名称、来源页面、Mindat 图片编号、原始文件名、筛选决策和本地路径等字段。图像筛选排除主体不清、混合矿物过多、严重模糊或曝光异常、人物或标尺遮挡过大以及不适合宏观图像识别的显微图片。

### 2.2 去重与数据划分

数据治理同时使用 SHA-256、Mindat 图片编号和感知哈希识别完全重复与近重复图片，并将同一重复组约束在单一数据划分中。最终训练集、验证集和测试集分别包含 5,961、1,284 和 1,284 张图像。训练阶段的数据增强只作用于训练集，验证集和测试集保持固定，以避免同源增强图像跨集合造成信息泄漏。

### 2.3 评价指标

总体指标采用 Accuracy 和 Macro F1。考虑选矿角色识别中的非对称风险，另报告目标类召回率、目标类漏选率、含钛干扰误入目标类比例和金属光泽干扰误入目标类比例。所有主要模型使用三个固定随机种子重复训练，并报告均值和样本标准差。固定测试集上的差异使用成对簇 Bootstrap 估计区间，抽样单元与数据分组保持一致。

## 3 RSG-HRGV-Net

### 3.1 层级双专家

设输入图像为 $x$，共享 EfficientNet-B0 主干为 $f_\theta$，其输出特征为

$$
h=f_\theta(x)\in\mathbb{R}^{1280}.
$$

直接角色专家和矿物种类专家分别输出四类角色后验和十七类矿物种类后验：

$$
p_d(y\mid x)=\operatorname{softmax}(W_dh),\qquad
p_s(k\mid x)=\operatorname{softmax}(W_sh).
$$

令 $A\in\{0,1\}^{4\times17}$ 为固定的矿物种类—选矿角色映射矩阵，且每一列只有一个元素为 1。种类专家可诱导出角色后验

$$
p_m(y\mid x)=A p_s(k\mid x).
$$

直接角色专家利用粗粒度角色监督，种类专家利用细粒度矿物监督。两者对同一图像的预测分歧还可以作为证据可靠性的显式信号。

### 3.2 可靠性门控

对概率向量 $p$ 定义归一化熵 $\bar H(p)$。可靠性门控以共享特征、两位专家的归一化熵和 Jensen-Shannon 分歧为输入：

$$
g(x)=\sigma\!\left(\operatorname{MLP}\left[h,\bar H(p_d),\bar H(p_m),D_{\mathrm{JS}}(p_d\Vert p_m)\right]\right).
$$

融合角色后验为

$$
p_f(y\mid x)=g(x)p_d(y\mid x)+[1-g(x)]p_m(y\mid x).
$$

该结构允许模型根据样本级不确定性选择更可信的证据路径，而不是使用固定融合系数。

### 3.3 双困难负样本验证与残差校正

网络设置两个二分类验证器：$v_{Ti}(x)$ 用于区分目标矿物与含钛干扰矿物，$v_{Met}(x)$ 用于区分目标矿物与金属光泽干扰矿物。验证器只在对应的目标—困难负样本子集上计算损失，使其学习针对性的局部决策边界。

为避免旧式乘法校正在验证器给出支持证据时仍持续改变目标后验，本文定义带中性区的残差惩罚：

$$
c_h(v_h)=\frac{[\tau_h-v_h]_+}{\tau_h},\qquad
\eta(x)=\beta_{Ti}c_{Ti}(v_{Ti})+\beta_{Met}c_{Met}(v_{Met}).
$$

若 $T$ 表示目标角色，则最终后验为

$$
q_T=\frac{p_{f,T}e^{-\eta}}{p_{f,T}e^{-\eta}+\sum_{j\ne T}p_{f,j}},
$$

$$
q_j=\frac{p_{f,j}}{p_{f,T}e^{-\eta}+\sum_{\ell\ne T}p_{f,\ell}},\qquad j\ne T.
$$

当两个验证器均达到中性阈值时，$\eta=0$ 且 $q=p_f$；只有验证器给出反对证据时，目标后验才被抑制。

### 3.4 训练目标

完整损失为

$$
\mathcal L=\mathcal L_{role}
+\lambda_d\mathcal L_{direct}
+\lambda_s\mathcal L_{species}
+\lambda_c\mathcal L_{KL}
+\lambda_v(\mathcal L_{Ti}+\mathcal L_{Met})
+\lambda_{con}\mathcal L_{contrast}
+\lambda_g\mathcal L_{reg}.
$$

$\mathcal L_{role}$ 监督最终后验，$\mathcal L_{direct}$ 和 $\mathcal L_{species}$ 分别监督两个专家，$\mathcal L_{KL}=D_{KL}(p_m\Vert p_d)$ 约束两条层级证据，$\mathcal L_{Ti}$ 和 $\mathcal L_{Met}$ 训练验证器，$\mathcal L_{contrast}$ 在表示空间中强化目标类与两类困难负样本的关系约束，$\mathcal L_{reg}$ 监督门控降低样本级专家选择后悔。

### 3.5 后悔监督门控

仅依靠熵和专家分歧训练门控时，融合损失并不直接说明当前样本应选择哪一位专家。为此，对真实角色 $y$ 定义

$$
\ell_d=-\log p_d(y\mid x),\qquad
\ell_m=-\log p_m(y\mid x),\qquad
\Delta=\ell_m-\ell_d.
$$

软门控目标和差距权重分别为

$$
g^*=\sigma(\Delta/T_r),\qquad
w=\tanh(|\Delta|/T_w),
$$

从而

$$
\mathcal L_{reg}=w\operatorname{BCE}(g,g^*).
$$

当直接角色专家在真实类别上的损失更小时，$g^*$ 趋近 1；当种类映射专家更可靠时，$g^*$ 趋近 0。权重 $w$ 降低两位专家近似等价样本的监督强度。该分支使用停止梯度的专家后验构造 $g^*$ 与 $w$，因此只更新门控参数，不允许专家为迎合门控目标而改变证据。

## 4 理论性质

### 4.1 凸融合对数损失上界

对任意真实角色 $y$ 和 $g\in[0,1]$，由 $-\log t$ 的凸性可得

$$
-\log p_{f,y}\leq g[-\log p_{d,y}]+(1-g)[-\log p_{m,y}].
$$

该式说明概率空间融合的样本对数损失不超过两个专家损失的对应凸上界，但不保证学习门控必然优于等权融合。

### 4.2 残差校正的恒等性和单调性

若所有验证器均满足 $v_h\geq\tau_h$，则 $q=p_f$。对任意非目标角色 $j$，有

$$
\frac{q_T}{q_j}=\frac{p_{f,T}}{p_{f,j}}e^{-\eta},\qquad
\frac{\partial}{\partial c_h}\log\frac{q_T}{q_j}=-\beta_h\leq0.
$$

任意两个非目标角色 $j,k$ 仍满足 $q_j/q_k=p_{f,j}/p_{f,k}$。因此，残差校正只在反对证据出现时单调降低目标角色相对赔率，并保持非目标角色之间的相对顺序。

### 4.3 严格验证式接纳的误收上界

设 $E_0$ 为基础分类器将某类困难负样本判为目标的事件，$E_h$ 为对应验证器错误放行的事件。若部署时只有两者同时成立才接纳为目标，则

$$
P(E_0\cap E_h)\leq\min\{P(E_0),P(E_h)\}.
$$

条件独立时，上式进一步化为二者概率的乘积。该结论适用于严格验证式接纳规则，不应扩大解释为软后验校正必然降低所有误收。

### 4.4 带拒识的贝叶斯最优决策

给定动作集合 $\mathcal A$、错误代价 $C(y,a)$ 和拒识代价 $c_{rej}$，直接动作的最小条件风险为

$$
R_{act}(x)=\min_{a\in\mathcal A}\sum_y C(y,a)q(y\mid x).
$$

逐点比较直接动作风险和拒识代价，可得到

$$
\delta^*(x)=
\begin{cases}
\operatorname*{arg\,min}_{a\in\mathcal A}\sum_y C(y,a)q(y\mid x), & R_{act}(x)\leq c_{rej},\\
\mathrm{reject}, & R_{act}(x)>c_{rej}.
\end{cases}
$$

该规则是给定后验与代价条件下的贝叶斯最优选择性决策。现有数据没有真实化验成本，本文只验证风险—覆盖率关系，不将其解释为工业送检成本最优。

### 4.5 验证器梯度隔离

若验证器输入使用 $\operatorname{stopgrad}(h)$，则链式法则给出

$$
\frac{\partial\mathcal L_v}{\partial\theta}=0,
$$

而验证器自身参数仍可更新。该性质用于构造耦合与隔离消融，不表示梯度隔离一定提高识别性能。

### 4.6 门控误差与路由后悔上界

令 $a=p_d(y\mid x)$、$b=p_m(y\mid x)$，且 $a,b\geq\varepsilon>0$。若 $g_o=\mathbb I(a\geq b)$ 是硬最优门控，$p_g=ga+(1-g)b$，则

$$
0\leq-\log p_g+\log\max(a,b)
\leq\frac{|g-g_o||a-b|}{\varepsilon}.
$$

该上界由 $-\log t$ 在 $[\varepsilon,1]$ 上的 Lipschitz 性质得到。它说明，门控错误在专家差距较大时造成更高后悔，因而差距加权不是任意的经验技巧。

### 4.7 软目标对硬最优门控的逼近

对 $\Delta\neq0$，令 $g_o=\mathbb I(\Delta\geq0)$，则

$$
|g^*-g_o|\leq\exp(-|\Delta|/T_r).
$$

因此，软目标在专家差距较大时指数逼近硬选择，在专家接近时保留不确定性。本文通过软目标、硬目标与无差距加权三组受控消融检验这一设计。

### 4.8 后悔监督的局部梯度隔离

若 $g^*$ 与 $w$ 由停止梯度的专家后验构造，则后悔监督关于专家参数的梯度为 0，而关于门控参数的梯度仍可非零。该性质只隔离 $\mathcal L_{reg}$ 分支，不切断角色、种类、验证器和对比损失对共享表示的训练。

## 5 实验设计

### 5.1 对比与消融

基础对比包括 ResNet50、EfficientNet-B0、加权交叉熵、Focal Loss、角色感知多任务网络和分层 EfficientNet-B0。HRGV 消融包括等权融合、移除角色感知对比约束、验证器梯度隔离、移除验证器以及旧乘法校正。所有正式配置使用随机种子 20260727、20260728 和 20260729。

### 5.2 统计分析

每个配置报告三个随机种子的均值和样本标准差。对使用相同固定测试集的模型，以图像分组为簇进行成对 Bootstrap，估计 Accuracy、Macro F1、目标召回和两类误入目标比例差异的 95% 区间。区间跨 0 的比较不表述为显著优势。

## 6 结果

### 6.1 HRGV 主要结果

| 设置 | Accuracy | Macro F1 | 目标召回 | 含钛误入目标 | 金属光泽误入目标 |
|---|---:|---:|---:|---:|---:|
| 耦合残差 HRGV | 76.48% ± 0.23% | 74.59% ± 0.34% | 72.38% ± 3.22% | 9.16% ± 1.13% | 11.55% ± 1.18% |
| 等权融合 | 75.91% ± 0.29% | 74.01% ± 0.33% | 73.04% ± 2.99% | 9.78% ± 1.47% | 12.31% ± 2.37% |
| 无角色感知对比约束 | 75.39% ± 0.86% | 73.51% ± 0.56% | 71.05% ± 2.65% | 9.78% ± 2.74% | 10.98% ± 2.15% |
| 梯度隔离残差 HRGV | 74.71% ± 1.56% | 72.99% ± 2.15% | 75.03% ± 4.30% | 10.05% ± 0.62% | 13.64% ± 2.05% |
| 仅门控、无验证器 | 76.74% ± 0.65% | 74.91% ± 0.41% | 74.37% ± 0.83% | 9.78% ± 1.47% | 11.74% ± 0.87% |
| 旧乘法验证 | 75.91% ± 0.83% | 74.25% ± 1.02% | 71.05% ± 1.80% | 9.91% ± 1.05% | 12.12% ± 0.87% |

耦合残差 HRGV 在三次训练中获得 76.48% 的平均 Accuracy 和 74.59% 的平均 Macro F1。学习门控相对等权融合的 Macro F1 提高 0.58 个百分点，含钛干扰和金属光泽干扰误入目标类的比例分别下降 0.61 和 0.76 个百分点，但对应成对区间跨 0。因此，这一比较支持样本级门控具有可用趋势，尚不足以证明其稳定优于等权融合。

### 6.2 角色感知对比约束

移除角色感知对比约束后，Accuracy 和 Macro F1 分别下降到 75.39% 和 73.51%。完整 HRGV 相对该消融的 Accuracy 提高 1.09 个百分点，95% 成对簇 Bootstrap 区间为 [0.08, 2.15]；Macro F1 提高 1.08 个百分点，区间为 [0.12, 2.06]。在当前实验中，这是区间未跨 0 的主要组件证据，说明根据角色和困难负样本关系构造表示约束比不区分类别关系的统一难样本加权更有针对性。

### 6.3 验证器与梯度耦合的风险取舍

两个验证器在各自的目标—困难负样本子集上取得约 0.91 至 0.92 的 ROC-AUC。完整 HRGV 相对 gate-only 将含钛干扰和金属光泽干扰误入目标的均值分别降低约 0.62 和 0.19 个百分点，同时目标召回下降约 1.99 个百分点。验证器因而表现为风险控制组件，而非无代价的总体性能增强模块。

梯度隔离版本的目标召回提高到 75.03%，但 Accuracy、Macro F1 和两类误入率均变差，且种子间波动增大。主模型据此采用耦合训练，使验证器监督能够参与共享表征学习；隔离版本保留为召回优先情景的受控消融。

### 6.4 与分层模型比较

HRGV 相对既有分层 EfficientNet-B0 的 Accuracy 和 Macro F1 均值分别提高 1.06 和 1.18 个百分点，含钛干扰和金属光泽干扰误入目标类的比例分别下降 1.77 和 2.84 个百分点，但目标召回下降 5.18 个百分点。主要成对区间跨 0，故不能据此宣称 HRGV 全面或显著优于分层模型。更准确的解释是，HRGV 改变了模型的风险分配：它减少困难负样本误入目标的倾向，同时提高了目标漏选风险。

### 6.5 RSG-HRGV 后悔监督的正式验证

为检验后悔监督是否改善了其直接优化的专家路由目标，完整 RSG-HRGV 与耦合残差 HRGV 使用相同数据划分、相同三个随机种子和相同验证器推理图；二者唯一的设计差异是 RSG 使用停止梯度的专家证据构造软目标与差距权重。单种子受控预实验比较了软目标、硬目标、无差距加权和取消局部梯度隔离，随后冻结软目标配置并进行三随机种子正式对照。

| 设置 | Accuracy | Macro F1 | 目标召回 | 含钛误入目标 | 金属光泽误入目标 | 一对一错路由正确率 | 平均路由后悔 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 耦合残差 HRGV | 76.48% ± 0.23% | 74.59% ± 0.34% | 72.38% ± 3.22% | 9.16% ± 1.13% | 11.55% ± 1.18% | 45.99% ± 4.82% | 9.86% ± 0.96% |
| 完整 RSG-HRGV | 76.45% ± 0.98% | 74.76% ± 0.90% | 72.91% ± 1.38% | 8.42% ± 1.54% | 11.17% ± 0.87% | 58.01% ± 1.76% | 8.09% ± 0.17% |

RSG 将一对一错子集的正确专家选择率提高 12.02 个百分点，平均路由后悔降低 1.77 个百分点；后者的 2,000 次成对簇 Bootstrap 95% 区间为 [−2.86, −0.69] 个百分点，未跨 0。Accuracy、Macro F1、目标召回和两类误入指标的对应区间均跨 0。因此，实验支持“后悔监督改善其定义的跨粒度专家路由风险”，而不支持“RSG 已显著提升总体四分类精度”。

### 6.6 后悔门控组件消融

在完整 RSG 的固定协议下，进一步以三个随机种子替换单一门控组件。将连续目标 $g^*=\sigma(\Delta/T_r)$ 改为硬目标 $\mathbb I(\Delta\geq0)$ 后，平均路由后悔增加 1.94 个百分点，成对两阶段簇 Bootstrap 95% 区间为 [1.30, 2.60] 个百分点，表明本数据上的连续目标优于对近似等价专家作二值化监督。去除差距权重后，路由后悔相对完整 RSG 的差异为 −0.20 个百分点，区间 [−0.62, 0.12] 跨 0；因此该权重保留为由后悔上界导出的风险建模设计，但不能被表述为已获得稳定性能增益。允许后悔分支对专家证据反向传播时，路由后悔下降 0.51 个百分点，区间 [−0.85, −0.16]，但 Macro F1 下降 1.58 个百分点，区间 [−4.10, 0.35]。这说明局部梯度隔离提供的是可证明的优化边界，并在经验上呈现路由风险与主任务稳定性之间的权衡，不能被误写为对全部指标必然有利的结论。

### 6.7 摄影者留出下的机制确认

随机固定测试虽然控制了重复和近重复图像，但仍可能保留摄影背景、布光和上传者风格。为此，从具有摄影者字段的 5,639 张图像中，以摄影者与重复图像组整体构建 3,947/846/846 的训练、验证和测试划分。缺失摄影者字段的 2,890 张图像被排除；各划分间摄影者与 `split_group_id` 均不交叉。试跑种子 20260728 仅用于确认执行和冻结流程，正式统计使用未参与筛选的独立种子 20260727、20260729 与 20260730。

| 设置 | Accuracy | Macro F1 | 目标召回 | 含钛误入目标 | 金属光泽误入目标 | 一对一错路由正确率 | 平均路由后悔 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 耦合残差 HRGV | 71.20% ± 0.69% | 68.99% ± 1.17% | 71.25% ± 6.31% | 12.69% ± 1.83% | 11.88% ± 3.96% | 45.19% ± 8.34% | 11.77% ± 0.63% |
| RSG-HRGV | 71.47% ± 1.14% | 69.25% ± 0.99% | 72.04% ± 2.64% | 11.40% ± 2.10% | 14.19% ± 3.02% | 53.69% ± 4.02% | 8.24% ± 0.80% |

RSG 的 Macro F1、目标召回和含钛干扰误入目标在均值上呈有利方向，但成对簇 Bootstrap 区间均跨 0；金属光泽干扰误入目标的均值反而上升。因此不能称其在来源变化下稳定改善总体角色分类。与理论命题直接对应的路由后悔则在三种子中均下降，RSG−HRGV 为 −3.53 个百分点，95% 区间为 [−4.75, −2.17] 个百分点，方向性重采样概率为 1.0000。该独立来源留出结果支持后悔监督对于跨粒度专家证据选择的机制泛化，但仍不是跨网站、未知矿物或工业生产线验证。

### 6.8 门控可靠性分层诊断与公式级重放

为避免常规逐图预测文件的六位小数导出将极小概率舍入为零，本文从各协议保存的最佳检查点只读重放两路专家、门控和融合后验，并以 15 位小数导出真值类概率。固定测试、摄影者留出和 ResNet50 主干替换各包含三个随机种子，共 9 次重放和 10,242 张图像。取 float32 概率下界为 \(1.19\times10^{-7}\)、数值容差为 \(2\times10^{-6}\)，凸融合后悔上界的最大残差为 0，软目标逼近界的最大残差为 \(5.94\times10^{-8}\)，两项违反数均为 0。ResNet50 的三个重放单独覆盖 3,852 张图像，违反数同样为 0。这一检查验证了所实现路由公式在两个卷积主干上的数值一致性，不是新的分类、工业分选或外部泛化结论。

统一概率下界会因极小真值类概率而产生过松的数值尺度。因而对第 \(i\) 个样本取 \(\epsilon_i=\min\{p_d(y_i\mid x_i),p_m(y_i\mid x_i)\}\)，并使用 B.1 的逐图直接推论 \(B_i^{\mathrm{loc}}=|g_i-g_{o,i}|\,|p_d(y_i\mid x_i)-p_m(y_i\mid x_i)|/\epsilon_i\) 进行诊断。按 \(B_i^{\mathrm{loc}}\) 的排序三分位数分层后，固定测试、摄影者留出和 ResNet50 协议的平均路由后悔分别从最低层的 0.00038、0.00141 和 0.00013 上升至最高层的 0.22702、0.22066 和 0.24624。该梯度表明局部上界能区分容易和困难的专家路由样本，但不代表其数值越大即存在新的类别错误模式。

对 B.2，以两位专家的真值类对数概率差作为证据差距，将样本分为三个分位层。三个协议的软目标与硬最优门控平均偏差在最小差距层分别为 0.49895、0.49622 和 0.49967，在最大差距层下降至 0.13979、0.13381 和 0.14212，并始终低于相应的指数上界。该现象与“专家证据接近时保留连续不确定性，差距增大时趋近硬选择”的设计相符。它使用固定检查点输出作描述性机制诊断，不能替代独立统计检验，也不应当作为总体分类性能提升的证据。

## 7 讨论

实验结果表明，钒钛矿相关矿物四类识别的主要困难并非简单的类别不平衡。Focal Loss 对不同类别的影响不一致，而角色感知对比约束在完整 HRGV 中取得了当前最清晰的组件贡献。这说明类别关系应由具体失效模式驱动，而不是统一提高所有难样本权重。

门控和验证器的作用需要与其理论边界一起理解。凸融合上界证明了概率融合的基本稳定性质，却不意味着学习门控必然提高测试集性能。残差校正的恒等性和单调性保证了网络只在反对证据区域抑制目标赔率，却不能排除验证器本身发生错误。验证器降低两类干扰误入的同时损失目标召回，正是这种不确定性在经验结果中的体现。

RSG 的理论价值不在于为每一个分类指标承诺正增益，而在于将跨粒度证据的选择误差转化为可定义、可度量的路由后悔。9 次高精度重放给出了 B.1 和 B.2 在实际实现中的零违反记录；局部 B.1 推论进一步显示，高路由后悔集中在理论上界也较高的样本层。该证据链将“网络结构--优化目标--可检验性质--经验诊断”连成闭环，但它仍只针对既定的两专家概率和冻结检查点，不构成新的泛化定理。

本研究仍存在四项限制。第一，图像来自公开矿物标本页面，不是生产线上的碎矿颗粒。第二，Mindat 页面主题矿物可能与伴生矿物和基质共同出现，单标签仍存在弱监督噪声。第三，钛磁铁矿真实样本数量有限，普通磁铁矿只能作为视觉代理。第四，当前固定划分虽控制了重复和近重复图片，但来源外泛化和独立未知矿物拒识仍需进一步验证。

现有元数据不足以把摄影者留出再扩大解释为矿区外或网站外测试。最终清单中只有 57.16% 的图像具有有效自由文本矿区字段，且至少 20 张图像并覆盖三个及以上角色的矿区组为零；高频矿区常与单一角色绑定。全部图像也仍来自 Mindat 生态，`source_type` 仅记录获取方式。因而本文不构造可能将矿区属性与类别标签混淆的矿区整体留出，而将摄影者整体留出限定为公开标本图像中摄影风格变化下的机制确认。真实外部验证需要独立采集、专家确认并标准化记录成像条件的颗粒或标本图像。

阶段条件化选矿决策图是更贴近真实流程的后续方向。同一矿物在铁钒回收、选钛和脱硫等阶段可能对应不同处理动作，但当前数据不包含流程阶段、品位、回收率、物料流和送检成本。缺少这些变量时，将阶段风险强行并入第一篇论文会引入不可验证假设。因此，本文将 HRGV 限定在静态选矿角色视觉识别，将阶段相关决策留待真实流程数据到位后研究。

## 8 结论

本文针对公开矿物标本图像中目标矿物与两类困难负样本的结构性混淆，提出 RSG-HRGV-Net。该模型通过层级双专家、后悔监督门控、双验证器和中性区残差校正联合建模矿物种类证据、选矿角色证据和困难负样本风险，并给出八项具有明确假设边界的理论性质。三随机种子实验中，RSG 将平均路由后悔降低 1.77 个百分点，成对簇 Bootstrap 95% 区间为 [−2.86, −0.69] 个百分点；总体 Accuracy 和 Macro F1 的差异区间均跨 0。角色感知对比约束仍对总体性能给出较清晰的组件支持。由此，本文的网络创新应定位为可证明、可测量的跨粒度证据路由机制，而不是无条件的分类精度突破。研究结果为公开标本条件下的钒钛矿相关矿物视觉识别提供了可复现方法基础，但不能替代真实矿石、品位和工业回收率验证。

## 数据与代码可用性（工作稿）

训练代码、数据清单、固定划分、实验配置、逐图预测和统计分析结果保存在项目 GitHub 仓库。受原始图片来源许可约束，仓库不直接分发 Mindat 原始图片；研究者可依据来源页面和元数据清单重建合法副本。

## 待补内容

- 逐篇核验后的引言与相关工作引用。
- 完整训练超参数表和硬件环境表。
- 来源留出、代理样本和选择性识别结果的精简整合。
- 独立 OOD 数据与开集协议。
- 目标期刊格式、参考文献、基金、作者贡献、利益冲突和 AI 使用声明。
